{
  "name": "desk-playwright-runner",
  "version": "1.0.0",
  "description": "Express server that runs Playwright smoke tests and streams output via SSE",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^26.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
